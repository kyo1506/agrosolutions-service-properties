# ⚠️ IMPORTANTE: Acesso Principal via Ocelot API Gateway
# Este serviço NÃO possui Ingress próprio.
# O acesso PÚBLICO à API deve ser feito via Ocelot Gateway em:
# - api.agrosolutions.site/properties/* → roteado para properties-api-service.agrosolutions-properties:80
#
# Configuração do Ocelot Gateway está em: agrosolutions-api-gateway/src/AgroSolutions.ApiGateway/ocelot.json
#
# Para acesso direto (debug), use kubectl port-forward:
#   kubectl port-forward -n agrosolutions-properties svc/properties-api-service 8080:80
